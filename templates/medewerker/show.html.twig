{% extends 'base.html.twig' %}

{% block title %}Medewerker{% endblock %}

{% block body %}
    <h1>Medewerker</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ medewerker.id }}</td>
            </tr>
            <tr>
                <th>Naam</th>
                <td>{{ medewerker.naam }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ medewerker.email }}</td>
            </tr>
            <tr>
                <th>Wachtwoord</th>
                <td>{{ medewerker.wachtwoord }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ medewerker.role }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('medewerker_index') }}">back to list</a>

    <a href="{{ path('medewerker_edit', {'id': medewerker.id}) }}">edit</a>

    {{ include('medewerker/_delete_form.html.twig') }}
{% endblock %}
